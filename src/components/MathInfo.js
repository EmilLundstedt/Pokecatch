import React from 'react'
import TextField from '@material-ui/core/TextField';

function MathInfo(equation) {
    return (
        <div>
            {equation.string}
        </div>
    )
}

export default MathInfo
